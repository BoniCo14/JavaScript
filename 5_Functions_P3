/*a function called properCaps that takes in a string and returns a new string where the first character is capitalized if it isn't already and should end in a period if it doesn't already.*/

let properCaps = (str) =>
{
  let firstLetter = str[0] //First letter of string
  
  if (firstLetter !== firstLetter.toUpperCase()) //To capitalize first letter
  { 
     
